name: mobile_drug_use_app
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0+3

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.5.0
  flutter_dotenv: ^6.0.0
  supabase_flutter: ^2.10.3  # Updated to latest version
  fl_chart: ^0.66.0
  intl: ^0.19.0 
  supabase: ^2.10.0  # Updated to match supabase_flutter
  shared_preferences: ^2.2.2
  provider: ^6.0.5
  uuid: ^4.2.0
  device_info_plus: ^10.1.0
  package_info_plus: ^8.0.2
  share_plus: ^10.1.2
  path_provider: ^2.1.5
  cryptography: ^2.7.0  # For AES-256-GCM encryption
  crypto: ^3.0.3  # For SHA-256 key derivation
  flutter_secure_storage: ^9.2.2  # For secure PIN storage (biometrics)
  local_auth: ^2.3.0  # For biometric authentication
  url_launcher: ^6.1.10  # Add other dependencies as needed
  app_links: ^6.3.3  # For deep link handling (email confirmation, password reset)
  freezed_annotation: ^2.4.1
  riverpod_annotation: ^2.6.1
  json_annotation: ^4.9.0
  go_router: ^17.0.1


dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^5.4.4  # For mocking
  build_runner: ^2.4.6  # To generate mocks
  freezed: ^2.4.6
  flutter_lints: ^5.0.0
  integration_test:
    sdk: flutter
  riverpod_generator: ^2.6.4
  json_serializable: ^6.9.5

flutter:
  uses-material-design: true
  assets:
    - .env
