import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder', () {
    // Intentionally empty.
    // We avoid networked Supabase calls in unit tests for determinism.
    expect(true, isTrue);
  });
}
